local DemoxLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/IngVzzz/Gui-Lybrary/refs/heads/main/project%20lybrary"))()
DemoxLib:Init("VIOLENCE DISTRICT 🩸")

-- ========== NO CLIP SCRIPT (ASLI) ==========
local PlayersNoclip = game:GetService("Players")
local LocalPlayerNoclip = PlayersNoclip.LocalPlayer
local RunServiceNoclip = game:GetService("RunService")

local noclipActive = false
local noclipConn = nil
local originalCanCollide = {}

local function saveOriginalCollision(character)
    if not character then return end
    originalCanCollide = {}
    for _, part in ipairs(character:GetDescendants()) do
        if part:IsA("BasePart") and part.CanCollide then
            originalCanCollide[part] = part.CanCollide
        end
    end
end

local function restoreOriginalCollision(character)
    for part, canCollide in pairs(originalCanCollide) do
        if part and part.Parent then
            pcall(function() part.CanCollide = canCollide end)
        end
    end
    originalCanCollide = {}
end

local function setNoclip(state)
    local character = LocalPlayerNoclip.Character
    if not character then return end
    if state then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA("BasePart") then
                if not originalCanCollide[part] then
                    originalCanCollide[part] = part.CanCollide
                end
                part.CanCollide = false
            end
        end
    else
        for part, canCollide in pairs(originalCanCollide) do
            if part and part.Parent then
                part.CanCollide = canCollide
            end
        end
        originalCanCollide = {}
    end
end

local function startNoclip()
    if noclipActive then return end
    noclipActive = true
    local character = LocalPlayerNoclip.Character
    if character then
        saveOriginalCollision(character)
        setNoclip(true)
    end
    local characterAddedConn = LocalPlayerNoclip.CharacterAdded:Connect(function(newChar)
        task.wait(0.1)
        if noclipActive then
            saveOriginalCollision(newChar)
            setNoclip(true)
        end
    end)
    noclipConn = RunServiceNoclip.RenderStepped:Connect(function()
        if not noclipActive then return end
        local char = LocalPlayerNoclip.Character
        if not char then return end
        for _, part in ipairs(char:GetDescendants()) do
            if part:IsA("BasePart") and part.CanCollide == true then
                part.CanCollide = false
            end
        end
    end)
    noclipConn.CharacterAdded = characterAddedConn
end

local function stopNoclip()
    if not noclipActive then return end
    noclipActive = false
    if noclipConn then
        if noclipConn.CharacterAdded then
            pcall(function() noclipConn.CharacterAdded:Disconnect() end)
        end
        noclipConn:Disconnect()
        noclipConn = nil
    end
    restoreOriginalCollision(LocalPlayerNoclip.Character)
end
-- ========== AKHIR NO CLIP ==========

-- ========== WALK SPEED SCRIPT ==========
local walkSpeedValue = 16
local walkSpeedActive = false
local defaultWalkSpeed = 16

local function setWalkSpeed(speed)
    walkSpeedValue = math.clamp(speed, 16, 250)
    local char = game.Players.LocalPlayer.Character
    if char and char:FindFirstChild("Humanoid") and walkSpeedActive then
        char.Humanoid.WalkSpeed = walkSpeedValue
    end
end

local function startWalkSpeed(speed)
    walkSpeedActive = true
    if speed then setWalkSpeed(speed) end
    local char = game.Players.LocalPlayer.Character
    if char and char:FindFirstChild("Humanoid") then
        defaultWalkSpeed = char.Humanoid.WalkSpeed
        char.Humanoid.WalkSpeed = walkSpeedValue
    end
end

local function stopWalkSpeed()
    walkSpeedActive = false
    local char = game.Players.LocalPlayer.Character
    if char and char:FindFirstChild("Humanoid") then
        char.Humanoid.WalkSpeed = defaultWalkSpeed
    end
end
-- ========== AKHIR WALK SPEED ==========

-- ========== BYPASS ZOOM SCRIPT (ASLI) ==========
local PlayersZoom = game:GetService("Players")
local LocalPlayerZoom = PlayersZoom.LocalPlayer
local RunServiceZoom = game:GetService("RunService")
local UserInputServiceZoom = game:GetService("UserInputService")
local CameraZoom = workspace.CurrentCamera

local zoomLevel = 70
local zoomSpeed = 3
local isZoomActive = false
local DEFAULT_FOV = 70
local renderZoomConn = nil
local inputZoomConn = nil
local maxZoomConn = nil

local function getCameraZoom()
    if workspace and workspace.CurrentCamera then
        CameraZoom = workspace.CurrentCamera
        return true
    end
    return false
end

local function disableZoomBypass()
    if not isZoomActive then return end
    isZoomActive = false
    if CameraZoom then pcall(function() CameraZoom.FieldOfView = DEFAULT_FOV end) end
    pcall(function()
        LocalPlayerZoom.CameraMaxZoomDistance = 40
        LocalPlayerZoom.CameraMinZoomDistance = 1
    end)
    if renderZoomConn then renderZoomConn:Disconnect() renderZoomConn = nil end
    if inputZoomConn then inputZoomConn:Disconnect() inputZoomConn = nil end
    if maxZoomConn then maxZoomConn:Disconnect() maxZoomConn = nil end
end

local function enableZoomBypass()
    if isZoomActive then return end
    if not getCameraZoom() then task.wait(0.3) end
    isZoomActive = true
    zoomLevel = CameraZoom.FieldOfView
    DEFAULT_FOV = CameraZoom.FieldOfView
    pcall(function()
        LocalPlayerZoom.CameraMaxZoomDistance = 50
        LocalPlayerZoom.CameraMinZoomDistance = 8
    end)
    maxZoomConn = LocalPlayerZoom:GetPropertyChangedSignal("CameraMaxZoomDistance"):Connect(function()
        if isZoomActive and LocalPlayerZoom.CameraMaxZoomDistance < 50 then
            pcall(function() LocalPlayerZoom.CameraMaxZoomDistance = 50 end)
        end
    end)
    inputZoomConn = UserInputServiceZoom.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then return end
        if not isZoomActive or not CameraZoom then return end
        if input.UserInputType == Enum.UserInputType.MouseWheel then
            zoomLevel = zoomLevel - (input.Position.Z * zoomSpeed)
            zoomLevel = math.clamp(zoomLevel, 20, 120)
            pcall(function() CameraZoom.FieldOfView = zoomLevel end)
        end
    end)
    renderZoomConn = RunServiceZoom.RenderStepped:Connect(function()
        if not isZoomActive or not CameraZoom then return end
        pcall(function()
            if CameraZoom.FieldOfView ~= zoomLevel then CameraZoom.FieldOfView = zoomLevel end
            if LocalPlayerZoom.CameraMaxZoomDistance ~= 50 then LocalPlayerZoom.CameraMaxZoomDistance = 50 end
        end)
    end)
end

local function startZoom() enableZoomBypass() end
local function stopZoom() disableZoomBypass() end
-- ========== AKHIR BYPASS ZOOM ==========

-- ========== CROUSHAIR SCRIPT (ASLI) ==========
local PlayersCross = game:GetService("Players")
local LocalPlayerCross = PlayersCross.LocalPlayer
local playerGui = LocalPlayerCross:WaitForChild("PlayerGui")
local crosshairGui = nil
local dot = nil
local croushairActive = false

local function CreateCroushair()
    if crosshairGui then return end
    crosshairGui = Instance.new("ScreenGui")
    crosshairGui.Name = "CustomCrosshair"
    crosshairGui.ResetOnSpawn = false
    crosshairGui.IgnoreGuiInset = true
    crosshairGui.Parent = playerGui
    dot = Instance.new("Frame")
    dot.Name = "Dot"
    dot.Size = UDim2.new(0, 5, 0, 5)
    dot.Position = UDim2.new(0.5, 0, 0.51, 0)
    dot.AnchorPoint = Vector2.new(0.5, 0.5)
    dot.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    dot.BorderSizePixel = 0
    dot.Parent = crosshairGui
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0.5, 0)
    corner.Parent = dot
end

local function RemoveCroushair()
    if crosshairGui then crosshairGui:Destroy() crosshairGui = nil end
end

local function startCroushair()
    if croushairActive then return end
    croushairActive = true
    CreateCroushair()
end

local function stopCroushair()
    if not croushairActive then return end
    croushairActive = false
    RemoveCroushair()
end
-- ========== AKHIR CROUSHAIR ==========

-- ========== MOONWALK SCRIPT (ASLI) ==========
local PlayersMoon = game:GetService("Players")
local LocalPlayerMoon = PlayersMoon.LocalPlayer
local RunServiceMoon = game:GetService("RunService")
local UISMoon = game:GetService("UserInputService")
local CoreGuiMoon = game:GetService("CoreGui")

local moonwalkChar, moonwalkHumanoid, moonwalkRootPart = nil, nil, nil
local currentGuiMoon, moonwalkButton = nil, nil
local moonwalkActive = false
local turningLeft, turningRight = false, false
local baseYaw, currentYaw = 0, 0
local maxTurn, turnSpeed, returnSpeed = 40, 850, 800
local moonwalkConnections, moonwalkButtonConnections = {}, {}
local lastCharCheck, charValid = 0, false
local moonwalkSystemActive = false

local function updateMoonwalkCharacterRefs(newChar)
    moonwalkChar = newChar or LocalPlayerMoon.Character
    if not moonwalkChar then charValid = false return false end
    moonwalkHumanoid = moonwalkChar:FindFirstChild("Humanoid")
    moonwalkRootPart = moonwalkChar:FindFirstChild("HumanoidRootPart")
    charValid = (moonwalkHumanoid and moonwalkRootPart and moonwalkRootPart.Parent ~= nil)
    return charValid
end

local function getYaw(cf) local _, y, _ = cf:ToEulerAnglesYXZ() return math.deg(y) end
local function setYaw(cf, yaw) return CFrame.new(cf.Position) * CFrame.Angles(0, math.rad(yaw), 0) end

local function clearMoonwalkButtonEvents()
    for _, conn in pairs(moonwalkButtonConnections) do pcall(function() conn:Disconnect() end) end
    moonwalkButtonConnections = {}
end

local function enableMoonwalk()
    if not charValid then if not updateMoonwalkCharacterRefs() then return end end
    if not moonwalkRootPart or not moonwalkHumanoid then return end
    if moonwalkActive then return end
    moonwalkActive = true
    moonwalkHumanoid.AutoRotate = false
    baseYaw = getYaw(moonwalkRootPart.CFrame)
    currentYaw = baseYaw
    if moonwalkButton then pcall(function() moonwalkButton.BackgroundColor3 = Color3.fromRGB(0, 170, 255) end) end
end

local function disableMoonwalk()
    if not moonwalkActive then return end
    moonwalkActive = false
    if charValid and moonwalkHumanoid then moonwalkHumanoid.AutoRotate = true end
    if moonwalkButton then pcall(function() moonwalkButton.BackgroundColor3 = Color3.fromRGB(35, 35, 35) end) end
    turningLeft, turningRight = false, false
end

local function destroyMoonwalkGUI()
    if currentGuiMoon then pcall(function() currentGuiMoon:Destroy() end) currentGuiMoon = nil end
    moonwalkButton = nil
end

local function createMoonwalkGUI()
    if currentGuiMoon then destroyMoonwalkGUI() end
    clearMoonwalkButtonEvents()
    local gui = Instance.new("ScreenGui")
    gui.Name = "MoonwalkController"
    gui.IgnoreGuiInset = true
    gui.ResetOnSpawn = false
    gui.Parent = CoreGuiMoon
    currentGuiMoon = gui
    moonwalkButton = Instance.new("TextButton")
    moonwalkButton.Size = UDim2.new(0, 50, 0, 50)
    moonwalkButton.Position = UDim2.new(1, -60, 1, -120)
    moonwalkButton.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    moonwalkButton.Text = "R"
    moonwalkButton.TextColor3 = Color3.new(1, 1, 1)
    moonwalkButton.TextScaled = true
    moonwalkButton.BorderSizePixel = 0
    moonwalkButton.Parent = gui
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(1, 0)
    corner.Parent = moonwalkButton
    local mouseDownConn = moonwalkButton.MouseButton1Down:Connect(enableMoonwalk)
    table.insert(moonwalkButtonConnections, mouseDownConn)
    local mouseUpConn = moonwalkButton.MouseButton1Up:Connect(disableMoonwalk)
    table.insert(moonwalkButtonConnections, mouseUpConn)
    local touchBeganConn = moonwalkButton.TouchBegan:Connect(enableMoonwalk)
    table.insert(moonwalkButtonConnections, touchBeganConn)
    local touchEndedConn = moonwalkButton.TouchEnded:Connect(disableMoonwalk)
    table.insert(moonwalkButtonConnections, touchEndedConn)
end

local function startMoonwalkSystem()
    if moonwalkSystemActive then return end
    moonwalkSystemActive = true
    clearMoonwalkButtonEvents()
    moonwalkConnections = {}
    local touchMovedConn = UISMoon.TouchMoved:Connect(function(input)
        if not moonwalkActive then return end
        if math.abs(input.Delta.X) > 3 then
            if input.Delta.X > 0 then turningRight, turningLeft = true, false
            else turningLeft, turningRight = true, false end
        end
    end)
    table.insert(moonwalkConnections, touchMovedConn)
    local touchEndedConn = UISMoon.TouchEnded:Connect(function() turningLeft, turningRight = false, false end)
    table.insert(moonwalkConnections, touchEndedConn)
    local inputBeganConn = UISMoon.InputBegan:Connect(function(input, gp)
        if gp then return end
        if input.KeyCode == Enum.KeyCode.R then enableMoonwalk() end
    end)
    table.insert(moonwalkConnections, inputBeganConn)
    local inputEndedConn = UISMoon.InputEnded:Connect(function(input, gp)
        if gp then return end
        if input.KeyCode == Enum.KeyCode.R then disableMoonwalk() end
    end)
    table.insert(moonwalkConnections, inputEndedConn)
    local renderConn = RunServiceMoon.RenderStepped:Connect(function(dt)
        if not moonwalkActive then return end
        local now = tick()
        if now - lastCharCheck >= 0.5 then
            lastCharCheck = now
            if not charValid then if not updateMoonwalkCharacterRefs() then return end end
        end
        if not moonwalkRootPart or not moonwalkHumanoid or not moonwalkRootPart.Parent then charValid = false return end
        if turningLeft then currentYaw = math.clamp(currentYaw - turnSpeed * dt, baseYaw - maxTurn, baseYaw + maxTurn)
        elseif turningRight then currentYaw = math.clamp(currentYaw + turnSpeed * dt, baseYaw - maxTurn, baseYaw + maxTurn)
        else
            if currentYaw < baseYaw then currentYaw = math.min(currentYaw + returnSpeed * dt, baseYaw)
            elseif currentYaw > baseYaw then currentYaw = math.max(currentYaw - returnSpeed * dt, baseYaw) end
        end
        if moonwalkRootPart and moonwalkRootPart.Parent then
            moonwalkRootPart.CFrame = setYaw(CFrame.new(moonwalkRootPart.CFrame.Position), currentYaw)
        end
    end)
    table.insert(moonwalkConnections, renderConn)
    local charAddedConn = LocalPlayerMoon.CharacterAdded:Connect(function(newChar)
        task.wait(0.2)
        updateMoonwalkCharacterRefs(newChar)
        if moonwalkActive then disableMoonwalk() task.wait(0.1) enableMoonwalk() end
    end)
    table.insert(moonwalkConnections, charAddedConn)
    updateMoonwalkCharacterRefs()
    createMoonwalkGUI()
end

local function stopMoonwalkSystem()
    moonwalkSystemActive = false
    destroyMoonwalkGUI()
    if moonwalkActive then disableMoonwalk() end
    for _, conn in ipairs(moonwalkConnections) do pcall(function() conn:Disconnect() end) end
    moonwalkConnections = {}
    clearMoonwalkButtonEvents()
end
-- ========== AKHIR MOONWALK ==========

-- ========== ESP PLAYER SCRIPT (STABILIZED) ==========
local PlayersESP = game:GetService("Players")
local LocalPlayerESP = PlayersESP.LocalPlayer
local ESP = { Players = { ["Killer"] = {Color = Color3.fromRGB(255, 0, 0), On = true}, ["Survivor"] = {Color = Color3.fromRGB(64, 224, 255), On = true} } }
local isESPActive, espConnections, playerConnections = false, {}, {}
local characterUpdateQueue = {} -- throttle queue

local function safeDisconnectESP(conn) pcall(function() if conn then conn:Disconnect() end end) end
local function clearESPConnections()
    for _, conn in pairs(espConnections) do safeDisconnectESP(conn) end
    espConnections = {}
    for player, conn in pairs(playerConnections) do safeDisconnectESP(conn) end
    playerConnections = {}
end

local function RemoveHighlight(char)
    if not char or not char.Parent then return end
    local h = char:FindFirstChild("ESP_Highlight")
    if h then h:Destroy() end
end

local function ApplyHighlight(char, color)
    if not char or not char.Parent or not color then return end
    local existing = char:FindFirstChild("ESP_Highlight")
    if existing and existing.FillColor == color then return end
    if existing then existing:Destroy() end
    local h = Instance.new("Highlight")
    h.Name = "ESP_Highlight"
    h.FillColor = color
    h.OutlineColor = color
    h.FillTransparency = 0.85
    h.OutlineTransparency = 0.35
    h.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
    h.Parent = char
end

local function UpdateSinglePlayer(player)
    if not isESPActive or player == LocalPlayerESP then return end
    if not player.Character or not player.Team then 
        if player.Character then RemoveHighlight(player.Character) end
        return 
    end
    local teamName = player.Team.Name
    local color = nil
    if teamName == "Killer" and ESP.Players["Killer"].On then color = ESP.Players["Killer"].Color
    elseif teamName == "Survivors" and ESP.Players["Survivor"].On then color = ESP.Players["Survivor"].Color end
    if color then ApplyHighlight(player.Character, color) else RemoveHighlight(player.Character) end
end

-- throttle untuk CharacterAdded
local function ThrottledUpdate(player)
    if characterUpdateQueue[player] then return end
    characterUpdateQueue[player] = true
    task.wait(0.2)
    characterUpdateQueue[player] = nil
    if isESPActive then UpdateSinglePlayer(player) end
end

local function BindPlayerEvents(player)
    if player == LocalPlayerESP then return end
    if playerConnections[player] then safeDisconnectESP(playerConnections[player]) end
    local conn = player.CharacterAdded:Connect(function() ThrottledUpdate(player) end)
    playerConnections[player] = conn
end

local function setupESPConnections()
    clearESPConnections()
    local playerAddedConn = PlayersESP.PlayerAdded:Connect(function(player) 
        BindPlayerEvents(player) 
        ThrottledUpdate(player)
    end)
    table.insert(espConnections, playerAddedConn)
    
    local playerRemovingConn = PlayersESP.PlayerRemoving:Connect(function(player)
        if player.Character then RemoveHighlight(player.Character) end
        if playerConnections[player] then safeDisconnectESP(playerConnections[player]) playerConnections[player] = nil end
    end)
    table.insert(espConnections, playerRemovingConn)
end

local function enableESP()
    if isESPActive then return end
    isESPActive = true
    setupESPConnections()
    local players = PlayersESP:GetPlayers()
    for i = 1, #players do
        local p = players[i]
        if p ~= LocalPlayerESP then 
            BindPlayerEvents(p)
            UpdateSinglePlayer(p)
        end
    end
end

local function disableESP()
    if not isESPActive then return end
    isESPActive = false
    clearESPConnections()
    local players = PlayersESP:GetPlayers()
    for i = 1, #players do
        if players[i].Character then RemoveHighlight(players[i].Character) end
    end
    characterUpdateQueue = {}
end
-- ========== AKHIR ESP (STABIL) ==========

-- ========== AIMBOT SCRIPT (VERSI BARU) ==========
local PlayersService = game:GetService("Players")
local LocalPlayer = PlayersService.LocalPlayer
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")

local aimbotCamera = workspace.CurrentCamera
local aimbotEnabled = false
local currentTarget = nil
local maxLockDistance = 35
local SMOOTHNESS = 0.85

local aimbotActiveConnections = {}
local aimbotUIConnections = {}
local aimbotButton = nil
local aimbotStroke = nil
local aimbotScreenGui = nil
local lastTargetUpdate = 0
local renderAimbotConnection = nil

local function cleanAimbotUIEvents()
    for _, conn in pairs(aimbotUIConnections) do
        pcall(function() conn:Disconnect() end)
    end
    aimbotUIConnections = {}
end

local function cleanAimbotEvents()
    for _, conn in pairs(aimbotActiveConnections) do
        pcall(function() conn:Disconnect() end)
    end
    aimbotActiveConnections = {}
    
    cleanAimbotUIEvents()
    
    if renderAimbotConnection then
        pcall(function() renderAimbotConnection:Disconnect() end)
        renderAimbotConnection = nil
    end
end

local function lockMouse()
    pcall(function()
        UserInputService.MouseBehavior = Enum.MouseBehavior.LockCenter
        UserInputService.MouseIconEnabled = false
    end)
end

local function unlockMouse()
    pcall(function()
        UserInputService.MouseBehavior = Enum.MouseBehavior.Default
        UserInputService.MouseIconEnabled = true
    end)
end

local function getClosestTarget()
    if not aimbotCamera then return nil end
    local closest = nil
    local shortestDist = math.huge
    local center = aimbotCamera.ViewportSize / 2
    local myChar = LocalPlayer.Character
    if not myChar then return nil end
    local myRoot = myChar:FindFirstChild("HumanoidRootPart")
    if not myRoot then return nil end
    
    for _, p in pairs(PlayersService:GetPlayers()) do
        if p ~= LocalPlayer then
            local char = p.Character
            if char then
                local root = char:FindFirstChild("HumanoidRootPart")
                local hum = char:FindFirstChild("Humanoid")
                if root and hum and hum.Health > 0 then
                    local dist3D = (myRoot.Position - root.Position).Magnitude
                    if dist3D <= maxLockDistance then
                        local screenPoint, onScreen = aimbotCamera:WorldToViewportPoint(root.Position)
                        if onScreen then
                            local screenDist = (Vector2.new(screenPoint.X, screenPoint.Y) - center).Magnitude
                            if screenDist < shortestDist then
                                closest = p
                                shortestDist = screenDist
                            end
                        end
                    end
                end
            end
        end
    end
    return closest
end

local function UpdateAimbotButtonUI()
    if not aimbotButton then return end
    pcall(function()
        if aimbotEnabled then
            aimbotButton.BackgroundColor3 = Color3.fromRGB(100, 255, 100)
            aimbotButton.BackgroundTransparency = 0.3
            if aimbotStroke then aimbotStroke.Color = Color3.fromRGB(100, 255, 100) end
        else
            aimbotButton.BackgroundColor3 = Color3.fromRGB(255, 80, 80)
            aimbotButton.BackgroundTransparency = 0.15
            if aimbotStroke then aimbotStroke.Color = Color3.fromRGB(255, 100, 100) end
        end
    end)
end

local function CreateAimbotGUI()
    cleanAimbotUIEvents()
    
    local existingGui = CoreGui:FindFirstChild("SimpleAimbot")
    if existingGui then
        pcall(function() existingGui:Destroy() end)
    end
    
    aimbotScreenGui = Instance.new("ScreenGui")
    aimbotScreenGui.Name = "SimpleAimbot"
    aimbotScreenGui.Parent = CoreGui

    aimbotButton = Instance.new("ImageButton")
    aimbotButton.Size = UDim2.new(0, 50, 0, 50)
    aimbotButton.Position = UDim2.new(1, -210, 1, -420)
    aimbotButton.AnchorPoint = Vector2.new(1, 1)
    aimbotButton.BackgroundColor3 = aimbotEnabled and Color3.fromRGB(100, 255, 100) or Color3.fromRGB(255, 80, 80)
    aimbotButton.BackgroundTransparency = aimbotEnabled and 0.3 or 0.15
    aimbotButton.Image = "rbxassetid://3926305904"
    aimbotButton.ImageColor3 = Color3.fromRGB(255, 255, 255)
    aimbotButton.ImageTransparency = 0.2
    aimbotButton.Parent = aimbotScreenGui

    local corner = Instance.new("UICorner", aimbotButton)
    corner.CornerRadius = UDim.new(1, 0)
    
    aimbotStroke = Instance.new("UIStroke", aimbotButton)
    aimbotStroke.Color = aimbotEnabled and Color3.fromRGB(100, 255, 100) or Color3.fromRGB(255, 100, 100)
    aimbotStroke.Thickness = 2
    aimbotStroke.Transparency = 0.5

    local dragging = false
    local dragStart = nil
    local startPos = nil

    local beganConn = aimbotButton.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            dragStart = input.Position
            startPos = aimbotButton.Position
        end
    end)
    table.insert(aimbotUIConnections, beganConn)

    local endedConn = aimbotButton.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = false
        end
    end)
    table.insert(aimbotUIConnections, endedConn)

    local changedConn = aimbotButton.InputChanged:Connect(function(input)
        if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
            local delta = input.Position - dragStart
            aimbotButton.Position = UDim2.new(
                startPos.X.Scale, 
                startPos.X.Offset + delta.X,
                startPos.Y.Scale,
                startPos.Y.Offset + delta.Y
            )
        end
    end)
    table.insert(aimbotUIConnections, changedConn)

    local clickConn = aimbotButton.MouseButton1Click:Connect(function()
        if dragging then
            dragging = false
            return
        end
        
        aimbotEnabled = not aimbotEnabled
        if aimbotEnabled then
            lockMouse()
            currentTarget = getClosestTarget()
        else
            unlockMouse()
            currentTarget = nil
        end
        UpdateAimbotButtonUI()
    end)
    table.insert(aimbotUIConnections, clickConn)
end

local function setupAimbotRenderStepped()
    if renderAimbotConnection then
        for i = #aimbotActiveConnections, 1, -1 do
            if aimbotActiveConnections[i] == renderAimbotConnection then
                table.remove(aimbotActiveConnections, i)
            end
        end
        pcall(function() renderAimbotConnection:Disconnect() end)
        renderAimbotConnection = nil
    end
    
    renderAimbotConnection = RunService.RenderStepped:Connect(function()
        if not aimbotEnabled then return end
        if not aimbotCamera then return end
        
        local now = tick()
        if now - lastTargetUpdate >= 0.1 then
            lastTargetUpdate = now
            if not currentTarget then
                currentTarget = getClosestTarget()
            elseif currentTarget and (not currentTarget.Character or not currentTarget.Character:FindFirstChild("HumanoidRootPart")) then
                currentTarget = getClosestTarget()
            end
        end
        
        if currentTarget and currentTarget.Character then
            local root = currentTarget.Character:FindFirstChild("HumanoidRootPart")
            if root then
                local lookAt = CFrame.new(aimbotCamera.CFrame.Position, root.Position)
                aimbotCamera.CFrame = aimbotCamera.CFrame:Lerp(lookAt, SMOOTHNESS)
            else
                currentTarget = nil
            end
        else
            currentTarget = nil
        end
    end)
    table.insert(aimbotActiveConnections, renderAimbotConnection)
end

local function setupAimbotCameraHandler()
    local cameraConn = workspace.Changed:Connect(function()
        if workspace.CurrentCamera then
            aimbotCamera = workspace.CurrentCamera
        end
    end)
    table.insert(aimbotActiveConnections, cameraConn)
end

local function startAimbot()
    if aimbotEnabled then return end
    
    if aimbotScreenGui then
        pcall(function() aimbotScreenGui:Destroy() end)
        aimbotScreenGui = nil
    end
    cleanAimbotEvents()
    
    aimbotEnabled = false
    aimbotCamera = workspace.CurrentCamera
    CreateAimbotGUI()
    setupAimbotRenderStepped()
    setupAimbotCameraHandler()
    
    if aimbotScreenGui then
        aimbotScreenGui.Enabled = true
    end
end

local function stopAimbot()
    if aimbotEnabled then
        aimbotEnabled = false
        unlockMouse()
        currentTarget = nil
    end
    
    if aimbotScreenGui then
        pcall(function() aimbotScreenGui:Destroy() end)
        aimbotScreenGui = nil
    end
    
    aimbotButton = nil
    aimbotStroke = nil
    cleanAimbotEvents()
end
-- ========== AKHIR AIMBOT ==========

-- ========== HITBOX SCRIPT (STABILIZED) ==========
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")

local hitboxPlayers = Players
local hitboxRunService = RunService
local hitboxLocalPlayer = LocalPlayer

local HITBOX_Size = 45
local OriginalHitboxSizes = {}
local hitboxActive = false
local hitboxUpdateTimer = 0
local hitboxActiveConnections = {}
local hitboxPlayerCharacterConns = {}

local function safeDisconnectHitbox(conn)
    pcall(function() if conn then conn:Disconnect() end end)
end

local function clearHitboxPlayerConns()
    for player, conn in pairs(hitboxPlayerCharacterConns) do
        safeDisconnectHitbox(conn)
    end
    hitboxPlayerCharacterConns = {}
end

local function clearHitboxConnections()
    for _, conn in pairs(hitboxActiveConnections) do
        safeDisconnectHitbox(conn)
    end
    hitboxActiveConnections = {}
    clearHitboxPlayerConns()
end

local function IsSurvivor(player)
    if player == hitboxLocalPlayer then return false end
    if not player.Team then return false end
    return player.Team.Name == "Survivors"
end

local function ResetHitbox(player)
    if not player then return end
    local orig = OriginalHitboxSizes[player]
    if orig and player.Character then
        local root = player.Character:FindFirstChild("HumanoidRootPart")
        if root and root.Size == Vector3.new(HITBOX_Size, HITBOX_Size, HITBOX_Size) then
            pcall(function()
                root.Size = orig
                root.CanCollide = true
            end)
        end
    end
    OriginalHitboxSizes[player] = nil
end

local function ResetAllHitbox()
    for p, origSize in pairs(OriginalHitboxSizes) do
        if p and p.Character then
            local root = p.Character:FindFirstChild("HumanoidRootPart")
            if root and root.Size == Vector3.new(HITBOX_Size, HITBOX_Size, HITBOX_Size) then
                pcall(function()
                    root.Size = origSize
                    root.CanCollide = true
                end)
            end
        end
    end
    OriginalHitboxSizes = {}
end

local function ExpandHitbox(player)
    if not hitboxActive then return end
    if not player or player == hitboxLocalPlayer then return end
    if not IsSurvivor(player) then return end
    
    local char = player.Character
    if not char then return end
    
    local root = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not root or not hum or hum.Health <= 0 then
        ResetHitbox(player)
        return
    end
    
    -- Cek apakah sudah di-expand
    if OriginalHitboxSizes[player] then 
        -- Already expanded, skip
        return 
    end
    
    OriginalHitboxSizes[player] = root.Size
    pcall(function()
        root.Size = Vector3.new(HITBOX_Size, HITBOX_Size, HITBOX_Size)
        root.CanCollide = false
    end)
end

local function UpdateAllHitboxes()
    for _, p in pairs(hitboxPlayers:GetPlayers()) do
        if p ~= hitboxLocalPlayer then
            ExpandHitbox(p)
        end
    end
end

local function BindHitboxPlayerEvents(player)
    if player == hitboxLocalPlayer then return end
    
    if hitboxPlayerCharacterConns[player] then
        safeDisconnectHitbox(hitboxPlayerCharacterConns[player])
        hitboxPlayerCharacterConns[player] = nil
    end
    
    local conn = player.CharacterAdded:Connect(function()
        task.wait(0.2)
        ExpandHitbox(player)
    end)
    hitboxPlayerCharacterConns[player] = conn
    table.insert(hitboxActiveConnections, conn)
end

local function setupHitboxConnections()
    clearHitboxConnections()
    
    local renderConn = hitboxRunService.RenderStepped:Connect(function(dt)
        if not hitboxActive then return end
        hitboxUpdateTimer = hitboxUpdateTimer + dt
        if hitboxUpdateTimer >= 0.3 then  -- turunin frekuensi dari 0.25 ke 0.3
            hitboxUpdateTimer = 0
            UpdateAllHitboxes()
        end
    end)
    table.insert(hitboxActiveConnections, renderConn)
    
    local playerAddedConn = hitboxPlayers.PlayerAdded:Connect(function(player)
        task.wait(0.1)
        BindHitboxPlayerEvents(player)
        ExpandHitbox(player)
    end)
    table.insert(hitboxActiveConnections, playerAddedConn)
    
    local playerRemovingConn = hitboxPlayers.PlayerRemoving:Connect(function(player)
        ResetHitbox(player)
        if hitboxPlayerCharacterConns[player] then
            safeDisconnectHitbox(hitboxPlayerCharacterConns[player])
            hitboxPlayerCharacterConns[player] = nil
        end
    end)
    table.insert(hitboxActiveConnections, playerRemovingConn)
end

-- MAIN FUNCTIONS (FIXED)
local function enableHitbox()
    if hitboxActive then return end
    setupHitboxConnections()
    UpdateAllHitboxes()
    hitboxActive = true
end

local function disableHitbox()
    if not hitboxActive then return end
    hitboxActive = false
    ResetAllHitbox()
    clearHitboxConnections()
end

-- Hapus fungsi duplikat
-- startHitbox() dan stopHitbox() dihapus, pakai enableHitbox/disableHitbox aja
-- ========== AKHIR HITBOX (STABIL) ==========

-- ========== DISABLE SKILLCHECK SCRIPT (STABILIZED - NO FUNCTION CHANGE) ==========
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local PlayersSkill = game:GetService("Players")
local LocalPlayerSkill = PlayersSkill.LocalPlayer

local isSkillKillActive = false
local activeSkillConnections = {}

local function safeDisconnectSkill(conn)
    pcall(function() if conn then conn:Disconnect() end end)
end

local function clearAllSkillConnections()
    for _, conn in pairs(activeSkillConnections) do
        safeDisconnectSkill(conn)
    end
    activeSkillConnections = {}
end

local function DestroyAllSkillChecks()
    local Remotes = ReplicatedStorage:FindFirstChild("Remotes")
    if not Remotes then return end
    
    for _, folderName in pairs({"Healing", "Generator"}) do
        local folder = Remotes:FindFirstChild(folderName)
        if folder then
            local skillCheck = folder:FindFirstChild("SkillCheckEvent")
            if skillCheck and skillCheck.Parent then  -- tambah cek Parent biar aman
                pcall(function() skillCheck:Destroy() end)
            end
        end
    end
end

local function enableSkillKill()
    if isSkillKillActive then return end
    isSkillKillActive = true
    DestroyAllSkillChecks()
    
    local remotes = ReplicatedStorage:FindFirstChild("Remotes")
    if remotes then
        for _, folderName in pairs({"Healing", "Generator"}) do
            local folder = remotes:FindFirstChild(folderName)
            if folder then
                local conn = folder.ChildAdded:Connect(function(child)
                    if child.Name == "SkillCheckEvent" and isSkillKillActive and child.Parent then  -- tambah cek Parent
                        pcall(function() child:Destroy() end)
                    end
                end)
                table.insert(activeSkillConnections, conn)
            end
        end
    end
end

local function disableSkillKill()
    if not isSkillKillActive then return end
    isSkillKillActive = false
    clearAllSkillConnections()
end
-- ========== AKHIR DISABLE SKILLCHECK ==========

-- ============================================
-- BUAT TAB
-- ============================================

local survivor = DemoxLib:Tab("SURVIVOR", "🏃")
local killer = DemoxLib:Tab("KILLER", "🔪")
local misc = DemoxLib:Tab("MISC", "⚙️")
local other = DemoxLib:Tab("OTHER", "🛠️")
local info = DemoxLib:Tab("INFO", "ℹ️")

-- ========== TAB SURVIVOR ==========
survivor:AddLabel("🏃 SURVIVOR SETTINGS", Color3.fromRGB(100, 200, 255))
survivor:AddSeparator()

local disableSkillcheck = false
survivor:AddToggle("DISABLE SKILLCHECK", function(state)
    disableSkillcheck = state
    if state then enableSkillKill() else disableSkillKill() end
end)

survivor:AddSeparator()

local espPlayer = false
survivor:AddToggle("ESP PLAYER", function(state)
    espPlayer = state
    if state then enableESP() else disableESP() end
end)

survivor:AddSeparator()

local moonwalkState = false
survivor:AddToggle("MOONWALK", function(state)
    moonwalkState = state
    if state then startMoonwalkSystem() else stopMoonwalkSystem() end
end)

survivor:AddSeparator()

local croushairState = false
survivor:AddToggle("CROUSHAIR", function(state)
    croushairState = state
    if state then startCroushair() else stopCroushair() end
end)

survivor:AddSeparator()

local bypassZoomState = false
survivor:AddToggle("BYPASS ZOOM", function(state)
    bypassZoomState = state
    if state then startZoom() else stopZoom() end
end)

-- ========== TAB KILLER ==========
killer:AddLabel("🔪 KILLER SETTINGS", Color3.fromRGB(255, 100, 100))
killer:AddSeparator()

local aimbotState = false
killer:AddToggle("AIMBOT", function(state)
    aimbotState = state
    if state then startAimbot() else stopAimbot() end
end)

killer:AddSeparator()

local hitboxState = false
killer:AddToggle("HITBOX", function(state)
    hitboxState = state
    if state then startExtremeHitbox(35) else stopExtremeHitbox() end
end)

-- ========== TAB MISC ==========
misc:AddLabel("⚙️ MISC SETTINGS", Color3.fromRGB(150, 150, 255))
misc:AddSeparator()

local fullbright = false
misc:AddToggle("FULLBRIGHT", function(state)
    fullbright = state
    local lighting = game:GetService("Lighting")
    if state then
        if not lighting:GetAttribute("OriginalBrightness") then
            lighting:SetAttribute("OriginalBrightness", lighting.Brightness)
            lighting:SetAttribute("OriginalClockTime", lighting.ClockTime)
            lighting:SetAttribute("OriginalAmbient", lighting.Ambient)
        end
        lighting.Brightness = 2
        lighting.ClockTime = 12
        lighting.Ambient = Color3.fromRGB(255, 255, 255)
        lighting.OutdoorAmbient = Color3.fromRGB(255, 255, 255)
        lighting.GlobalShadows = false
        lighting.FogEnd = 100000
    else
        lighting.Brightness = lighting:GetAttribute("OriginalBrightness") or 0.5
        lighting.ClockTime = lighting:GetAttribute("OriginalClockTime") or 0
        lighting.Ambient = lighting:GetAttribute("OriginalAmbient") or Color3.fromRGB(0, 0, 0)
        lighting.OutdoorAmbient = Color3.fromRGB(127, 127, 127)
        lighting.GlobalShadows = true
    end
end)

misc:AddSeparator()

local removeFog = false
misc:AddToggle("REMOVE FOG", function(state)
    removeFog = state
    local lighting = game:GetService("Lighting")
    if state then
        if not lighting:GetAttribute("OriginalFogEnd") then lighting:SetAttribute("OriginalFogEnd", lighting.FogEnd) end
        lighting.FogEnd = 100000
        lighting.FogStart = 100000
    else
        lighting.FogEnd = lighting:GetAttribute("OriginalFogEnd") or 1000
        lighting.FogStart = 0
    end
end)

-- ========== TAB OTHER ==========
other:AddLabel("🦶 WALK SPEED", Color3.fromRGB(100, 255, 100))
other:AddSeparator()

local walkSpeedToggleState = false
other:AddToggle("WALK SPEED", function(state)
    walkSpeedToggleState = state
    if state then startWalkSpeed(walkSpeedValue) else stopWalkSpeed() end
end)

other:AddSlider("Speed Value", 16, 250, 16, function(val)
    walkSpeedValue = val
    if walkSpeedToggleState then setWalkSpeed(val) end
end)

other:AddSeparator()

other:AddLabel("🌀 NO CLIP", Color3.fromRGB(255, 200, 100))
other:AddSeparator()

local noclipState = false
other:AddToggle("NO CLIP", function(state)
    noclipState = state
    if state then startNoclip() else stopNoclip() end
end)

-- ========== TAB INFO ==========
info:AddLabel("ℹ️ TENTANG SCRIPT", Color3.fromRGB(255, 200, 100))
info:AddSeparator()
info:AddLabel("📌 DEMOX GUI LIBRARY v3.3", Color3.fromRGB(180, 180, 180))
info:AddLabel("🔧 By: Demox_Ai'CvA057xp", Color3.fromRGB(180, 180, 180))
info:AddLabel("🎮 Game: Violence District", Color3.fromRGB(180, 180, 180))

info:AddSeparator()
info:AddLabel("🛠️ FITUR YANG TERSEDIA", Color3.fromRGB(100, 200, 255))
info:AddSeparator()
info:AddLabel("• DISABLE SKILLCHECK ✅", Color3.fromRGB(150, 150, 150))
info:AddLabel("• ESP PLAYER ✅", Color3.fromRGB(150, 150, 150))
info:AddLabel("• MOONWALK ✅", Color3.fromRGB(150, 150, 150))
info:AddLabel("• CROUSHAIR ✅", Color3.fromRGB(150, 150, 150))
info:AddLabel("• BYPASS ZOOM ✅", Color3.fromRGB(150, 150, 150))
info:AddLabel("• AIMBOT ✅", Color3.fromRGB(150, 150, 150))
info:AddLabel("• HITBOX ✅", Color3.fromRGB(150, 150, 150))
info:AddLabel("• FULLBRIGHT ✅", Color3.fromRGB(150, 150, 150))
info:AddLabel("• REMOVE FOG ✅", Color3.fromRGB(150, 150, 150))
info:AddLabel("• WALK SPEED + SLIDER ✅", Color3.fromRGB(150, 150, 150))
info:AddLabel("• NO CLIP ✅", Color3.fromRGB(150, 150, 150))

info:AddSeparator()
info:AddLabel("⚠️ PERINGATAN", Color3.fromRGB(255, 100, 100))
info:AddSeparator()
info:AddLabel("Gunakan script ini dengan bijak!", Color3.fromRGB(255, 150, 150))
info:AddLabel("Resiko banned ditanggung sendiri.", Color3.fromRGB(255, 150, 150))

info:AddSeparator()
info:AddButton("Tutup Menu", function()
    DemoxLib:ToggleGUI()
end)
